import React,{useCallback,useEffect,useMemo,useState}from"react";
import{supabase}from"@/lg/supabase";
import{C,DAYS}from"@/lg/data";
import{LGLogo,Card,Badge,Sec,Shell,AppBar}from"@/lg/ui";
const dayName=n=>DAYS[Math.max(0,Math.min(6,Number(n||1)-1))]||"Monday";
export function ParentApp({user,onLogout}){const[children,setChildren]=useState([]),[selectedId,setSelectedId]=useState(null),[batches,setBatches]=useState([]),[timetable,setTimetable]=useState([]),[attendance,setAttendance]=useState([]),[homework,setHomework]=useState([]),[materials,setMaterials]=useState([]),[fees,setFees]=useState([]),[tab,setTab]=useState("home"),[loading,setLoading]=useState(true),[error,setError]=useState("");const selected=useMemo(()=>children.find(c=>c.id===selectedId)||children[0]||null,[children,selectedId]);const load=useCallback(async()=>{setLoading(true);setError("");try{const{data:linkRows,error:linkError}=await supabase.from("parent_student_links").select("student_id,status").eq("status","active");if(linkError)throw linkError;const ids=[...new Set((linkRows||[]).map(r=>r.student_id).filter(Boolean))],fallback=user?.ref?[String(user.ref)]:[],studentIds=ids.length?ids:fallback;if(!studentIds.length)throw new Error("No student is linked to this parent account.");const{data:studentRows,error:studentError}=await supabase.from("students").select("*").in("id",studentIds);if(studentError)throw studentError;const safeStudents=(studentRows||[]).filter(s=>studentIds.includes(String(s.id)));setChildren(safeStudents);setSelectedId(current=>current&&safeStudents.some(s=>s.id===current)?current:safeStudents[0]?.id||null);const{data:memberships,error:membershipError}=await supabase.from("batch_students").select("batch_id,student_id,status").in("student_id",studentIds).eq("status","active");if(membershipError)throw membershipError;const batchIds=[...new Set((memberships||[]).map(m=>m.batch_id).filter(Boolean))];if(!batchIds.length){setBatches([]);setTimetable([]);setHomework([]);setMaterials([])}else{const[{data:batchRows,error:batchError},{data:ttRows,error:ttError},{data:hwRows,error:hwError},{data:materialRows,error:materialError}]=await Promise.all([supabase.from("batches").select("id,name,cls,sec,status").in("id",batchIds),supabase.from("timetable_entries").select("*").in("batch_id",batchIds).eq("status","active"),supabase.from("homework").select("*").in("batch_id",batchIds).order("created_at",{ascending:false}),supabase.from("materials").select("id,title,name,folder_id,storage_path,file_size,mime_type,created_at,batch_id").in("batch_id",batchIds).order("created_at",{ascending:false})]);if(batchError)throw batchError;if(ttError)throw ttError;if(hwError)throw hwError;if(materialError)throw materialError;setBatches((memberships||[]).map(m=>({...m,batch:(batchRows||[]).find(b=>String(b.id)===String(m.batch_id))||null})));setTimetable(ttRows||[]);setHomework(hwRows||[]);setMaterials(materialRows||[])}const[{data:attRows,error:attError},{data:feeRows,error:feeError}]=await Promise.all([supabase.from("attendance").select("*").in("sid",studentIds).order("date",{ascending:false}),supabase.from("fees").select("*").in("sid",studentIds).order("due",{ascending:true})]);if(attError)throw attError;if(feeError)throw feeError;setAttendance(attRows||[]);setFees(feeRows||[])}catch(e){console.error("Parent portal load failed:",e);setError(e?.message||"Unable to load your child's institute data.")}finally{setLoading(false)}},[user?.ref]);useEffect(()=>{void load()},[load]);const childBatches=batches.filter(m=>String(m.student_id)===String(selected?.id)),childBatchIds=new Set(childBatches.map(m=>String(m.batch_id))),childTimetable=timetable.filter(t=>childBatchIds.has(String(t.batch_id))),childHomework=homework.filter(h=>childBatchIds.has(String(h.batch_id))||(!h.batch_id&&h.cls===selected?.cls&&h.sec===selected?.sec)),childMaterials=materials.filter(m=>childBatchIds.has(String(m.batch_id))),childAttendance=attendance.filter(a=>String(a.sid)===String(selected?.id)),childFees=fees.filter(f=>String(f.sid)===String(selected?.id)),rate=childAttendance.length?Math.round(childAttendance.filter(a=>a.status==="present").length/childAttendance.length*100):0;const downloadMaterial=async material=>{if(!material?.storage_path)return;const{data,error:urlError}=await supabase.storage.from("materials").createSignedUrl(material.storage_path,3600,{download:material.name||material.title||"study-material"});if(urlError){setError(urlError.message);return}window.open(data.signedUrl,"_blank","noopener,noreferrer")};const content=loading?<Card>Loading your child's data…</Card>:error?<Card><div style={{color:C.red,fontWeight:700}}>{error}</div><button onClick={()=>void load()} style={{marginTop:12,padding:"10px 14px",borderRadius:10,border:0}}>Try again</button></Card>:!selected?<Card>No student is linked to this parent account.</Card>:<div>{children.length>1&&<Card style={{marginBottom:14}}><div style={{fontSize:12,color:C.sub,marginBottom:8}}>Children</div><select value={selected.id} onChange={e=>setSelectedId(e.target.value)} style={{width:"100%",padding:11,borderRadius:10,border:`1px solid ${C.border}`}}>{children.map(c=><option key={c.id} value={c.id}>{c.name} · {c.sid}</option>)}</select></Card>}<Card style={{marginBottom:14,background:"linear-gradient(135deg,#22C55E,#16A34A)",border:"none"}}><div><div style={{color:"#fff",fontSize:18,fontWeight:900}}>{selected.name}</div><div style={{color:"rgba(255,255,255,.75)",fontSize:12}}>Class {selected.cls}-{selected.sec} · {selected.sid}</div></div><div style={{display:"flex",gap:8,marginTop:12}}>{[[`${rate}%`,"Attendance"],[childHomework.length,"Homework"],[childFees.filter(f=>f.status!=="paid").length,"Fee Dues"]].map(([v,l])=><div key={l} style={{flex:1,background:"rgba(255,255,255,.15)",borderRadius:10,padding:8,textAlign:"center"}}><div style={{color:"#fff",fontWeight:900}}>{v}</div><div style={{color:"rgba(255,255,255,.7)",fontSize:10}}>{l}</div></div>)}</div></Card>{tab==="home"&&<><Sec title="My Batches"/>{childBatches.map(m=><Card key={m.batch_id} style={{marginBottom:8}}><div style={{fontWeight:800}}>{m.batch?.name||m.batch_id}</div><div style={{fontSize:12,color:C.sub}}>Class {m.batch?.cls||selected.cls}-{m.batch?.sec||selected.sec}</div></Card>)}</>}{tab==="timetable"&&<><Sec title="Timetable"/>{[...childTimetable].sort((a,b)=>Number(a.day_of_week)-Number(b.day_of_week)||String(a.start_time).localeCompare(String(b.start_time))).map(t=><Card key={t.id} style={{marginBottom:8}}><div style={{fontWeight:800}}>{dayName(t.day_of_week)} · {String(t.start_time||"").slice(0,5)}–{String(t.end_time||"").slice(0,5)}</div><div style={{fontSize:13,marginTop:4}}>{t.subject_name||t.subject||"Class"}</div></Card>)}</>}{tab==="attendance"&&<><Sec title="Attendance"/>{childAttendance.map(a=><Card key={a.id} style={{marginBottom:7,display:"flex",justifyContent:"space-between"}}><span>{a.date}</span><Badge label={a.status}/></Card>)}</>}{tab==="homework"&&<><Sec title="Homework"/>{childHomework.map(h=><Card key={h.id} style={{marginBottom:9}}><Badge label={h.subject||"Subject"}/><div style={{fontWeight:800,marginTop:7}}>{h.desc||h.title||"Homework"}</div><div style={{fontSize:11,color:C.sub}}>Due: {h.due||"—"}</div></Card>)}</>}{tab==="materials"&&<><Sec title="Study Materials"/>{childMaterials.map(m=><Card key={m.id} style={{marginBottom:9,display:"flex",justifyContent:"space-between",alignItems:"center",gap:10}}><div><div style={{fontWeight:800}}>{m.name||m.title||"Study material"}</div><div style={{fontSize:11,color:C.sub}}>{m.mime_type||"File"}</div></div><button onClick={()=>void downloadMaterial(m)} style={{border:0,borderRadius:9,padding:"9px 12px",background:C.accent,color:"#fff",fontWeight:800}}>Download</button></Card>)}</>}{tab==="fees"&&<><Sec title="Fees"/>{childFees.map(f=><Card key={f.id} style={{marginBottom:8,display:"flex",justifyContent:"space-between"}}><div><div style={{fontWeight:800}}>{f.desc||"Fee"}</div><div style={{fontSize:11,color:C.sub}}>Due: {f.due||"—"}</div></div><Badge label={f.status}/></Card>)}</>}</div>;const tabs=[{key:"home",icon:"🏠",label:"Home"},{key:"timetable",icon:"🗓️",label:"Classes"},{key:"attendance",icon:"✅",label:"Attend."},{key:"homework",icon:"📝",label:"HW"},{key:"materials",icon:"📚",label:"Materials"},{key:"fees",icon:"💰",label:"Fees"}];return <Shell header={<AppBar name={user.name} role="parent" userId={user.id} onLogout={onLogout}/>} tabs={tabs} activeTab={tab} setTab={setTab}>{content}</Shell>}
export{LGLogo};
