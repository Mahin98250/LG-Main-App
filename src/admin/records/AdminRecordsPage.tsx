import { useCallback, useEffect, useMemo, useState } from "react";
import { C, addR, delR, gdb, updR } from "@/lg/data";
import { supabase } from "@/lg/supabase";

type Row = Record<string, any> & { id: string | number };
type Kind = "students" | "teachers";
type Form = Record<string, string>;
type ProvisionRole = "student" | "parent" | "teacher";
type ProvisionResult = { authId?: string; email?: string; deleted?: boolean; repaired?: boolean };
const DEFAULT_STUDENT_PASSWORD = "Student@1234";
const DEFAULT_TEACHER_PASSWORD = "Teacher@1234";
const DEFAULT_PARENT_PASSWORD = "Parent@1234";
const TEACHER_SUBJECTS = ["English", "Social Studies", "Mathematics", "Science", "Hindi", "Gujarati", "Computer Science", "Accountancy", "Business Studies", "Economics", "Applied Mathematics", "Informatics Practices", "Entrepreneurship", "Physical Education", "Legal Studies", "Psychology"];
const CLASSES = ["9", "10", "11", "12"];
const SECTIONS = ["A", "B", "C", "D"];
const card: React.CSSProperties = { background: "#fff", border: `1px solid ${C.border}`, borderRadius: 20, boxShadow: "0 4px 20px rgba(15,27,61,.07)" };
async function provision(role: ProvisionRole, loginId: string, name: string, ref: string, action: "create" | "update" | "delete", authId?: string | null, password?: string): Promise<ProvisionResult> { const body: Record<string, unknown> = { action, role, loginId, name, ref }; if (authId) body.authId = authId; if (password) body.password = password; const { data, error } = await supabase.functions.invoke("admin-provision-user", { body }); if (error) throw new Error(error.message || "Authentication service failed."); if (data?.error) throw new Error(String(data.error)); return data as ProvisionResult; }
function validatePassword(password: string, label: string) { if (password.length < 8) throw new Error(`${label} must be at least 8 characters.`); }
function normalizePhone(value: string) { return value.replace(/\s+/g, "").trim(); }
async function nextStudentSid() { const rows = (await gdb("students")) as Row[]; const used = new Set(rows.map((r) => String(r.sid ?? "").trim().toUpperCase())); let max = 0; for (const row of rows) { const match = String(row.sid ?? "").match(/^LG-?(\d+)$/i); if (match) max = Math.max(max, Number(match[1])); } let n = max + 1; while (used.has(`LG-${String(n).padStart(3, "0")}`)) n += 1; return `LG-${String(n).padStart(3, "0")}`; }
async function nextTeacherTid() { const rows = (await gdb("teachers")) as Row[]; const used = new Set(rows.map((r) => String(r.tid ?? "").trim().toUpperCase())); let max = 0; for (const row of rows) { const match = String(row.tid ?? "").match(/^LGT-?(\d+)$/i); if (match) max = Math.max(max, Number(match[1])); } let n = max + 1; while (used.has(`LGT${String(n).padStart(2, "0")}`)) n += 1; return `LGT${String(n).padStart(2, "0")}`; }
function Input({ label, value, onChange, placeholder, required, type = "text", disabled = false, note }: { label: string; value: string; onChange: (v: string) => void; placeholder?: string; required?: boolean; type?: string; disabled?: boolean; note?: string }) { return <label style={{ display: "block", minWidth: 0 }}><span style={{ display: "block", fontSize: 12, fontWeight: 750, color: C.sub, marginBottom: 6 }}>{label}{required && <span style={{ color: C.red }}> *</span>}</span><input type={type} required={required} disabled={disabled} value={value} onChange={(e) => onChange(e.target.value)} placeholder={placeholder} style={{ width: "100%", boxSizing: "border-box", padding: "11px 13px", border: `1.5px solid ${C.border}`, borderRadius: 11, background: disabled ? "#F1F5F9" : "#F8FAFF", color: C.text, outline: "none" }} />{note && <span style={{ display: "block", marginTop: 5, fontSize: 10, color: C.sub }}>{note}</span>}</label>; }
function Select({ label, value, onChange, options, required }: { label: string; value: string; onChange: (v: string) => void; options: string[]; required?: boolean }) { return <label style={{ display: "block", minWidth: 0 }}><span style={{ display: "block", fontSize: 12, fontWeight: 750, color: C.sub, marginBottom: 6 }}>{label}{required && <span style={{ color: C.red }}> *</span>}</span><select required={required} value={value} onChange={(e) => onChange(e.target.value)} style={{ width: "100%", boxSizing: "border-box", padding: "11px 13px", border: `1.5px solid ${C.border}`, borderRadius: 11, background: "#F8FAFF", color: C.text, outline: "none" }}><option value="">Select…</option>{options.map((o) => <option key={o} value={o}>{o}</option>)}</select></label>; }
function MultiSelect({ label, value, onChange, options }: { label: string; value: string[]; onChange: (v: string[]) => void; options: string[] }) { return <div><span style={{ display: "block", fontSize: 12, fontWeight: 750, color: C.sub, marginBottom: 6 }}>{label}</span><div style={{ display: "flex", flexWrap: "wrap", gap: 7, padding: 10, border: `1.5px solid ${C.border}`, borderRadius: 11, background: "#F8FAFF", maxHeight: 220, overflowY: "auto" }}>{options.map((o) => { const checked = value.includes(o); return <button key={o} type="button" onClick={() => onChange(checked ? value.filter((x) => x !== o) : [...value, o])} style={{ border: `1px solid ${checked ? C.accent : C.border}`, borderRadius: 9, padding: "7px 10px", background: checked ? "#EEF2FF" : "#fff", color: checked ? C.accent : C.sub, fontWeight: checked ? 800 : 600, cursor: "pointer", fontSize: 11 }}>{checked ? "✓ " : ""}{o}</button>; })}</div></div>; }

export default function AdminRecordsPage({ kind }: { kind: Kind }) {
  const [rows, setRows] = useState<Row[]>([]); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); const [success, setSuccess] = useState(""); const [query, setQuery] = useState(""); const [editing, setEditing] = useState<Row | null>(null); const [form, setForm] = useState<Form>({}); const [teacherSubjects, setTeacherSubjects] = useState<string[]>([]); const [teacherClasses, setTeacherClasses] = useState<string[]>([]); const [credentials, setCredentials] = useState<{ role: string; login: string; password: string } | null>(null); const isStudent = kind === "students";
  const load = useCallback(async () => { setLoading(true); setError(""); try { setRows((await gdb(kind)) as Row[]); } catch (e) { setError(e instanceof Error ? e.message : "Unable to load records."); } finally { setLoading(false); } }, [kind]);
  useEffect(() => { void load(); }, [load]);
  const filtered = useMemo(() => { const q = query.trim().toLowerCase(); if (!q) return rows; return rows.filter((r) => Object.values(r).some((v) => String(v ?? "").toLowerCase().includes(q))); }, [query, rows]);
  const set = (key: string, value: string) => setForm((f) => ({ ...f, [key]: value }));
  const openNew = async () => { setEditing(null); setError(""); setSuccess(""); setCredentials(null); setTeacherSubjects([]); setTeacherClasses([]); try { if (isStudent) setForm({ name: "", sid: await nextStudentSid(), cls: "", sec: "", enroll: new Date().toISOString().slice(0, 10), status: "active", password: DEFAULT_STUDENT_PASSWORD, parentName: "", parentPhone: "" }); else setForm({ name: "", tid: await nextTeacherTid(), phone: "", status: "active", password: DEFAULT_TEACHER_PASSWORD }); } catch (e) { setError(e instanceof Error ? e.message : "Unable to prepare the form."); } };
  const openEdit = (row: Row) => { setEditing(row); setError(""); setSuccess(""); setCredentials(null); if (isStudent) setForm({ name: String(row.name ?? ""), sid: String(row.sid ?? ""), cls: String(row.cls ?? ""), sec: String(row.sec ?? ""), enroll: String(row.enroll ?? ""), status: String(row.status ?? "active"), password: "", parentName: String(row.parentname ?? ""), parentPhone: String(row.parentphone ?? "") }); else { setTeacherSubjects(String(row.subject ?? "").split(",").map((x) => x.trim()).filter(Boolean)); setTeacherClasses(Array.isArray(row.classes) ? row.classes.map(String) : []); setForm({ name: String(row.name ?? ""), tid: String(row.tid ?? ""), phone: String(row.phone ?? ""), status: String(row.status ?? "active"), password: "" }); } };
  const closeForm = () => { if (saving) return; setForm({}); setEditing(null); setTeacherSubjects([]); setTeacherClasses([]); setError(""); };
  const rollbackAuth = async (created: Array<{ role: ProvisionRole; authId?: string; login: string; name: string; ref: string }>) => { for (const item of created.reverse()) { if (!item.authId) continue; try { await provision(item.role, item.login, item.name, item.ref, "delete", item.authId); } catch (cleanupError) { console.error("Auth rollback failed:", cleanupError); } } };
  const save = async () => {
    if (!form.name?.trim()) { setError("Full Name is required."); return; }
    setSaving(true); setError(""); setSuccess(""); setCredentials(null);
    try {
      if (editing) {
        if (isStudent) {
          const parentPhone = normalizePhone(form.parentPhone ?? ""); if (!form.cls || !form.sec || !form.parentName?.trim() || !parentPhone) throw new Error("Fill all required student and parent fields."); if (form.password) validatePassword(form.password, "Student password");
          const users = (await gdb("users")) as Row[]; const su = users.find((u) => String(u.ref) === String(editing.id) && u.role === "student"); const pu = users.find((u) => String(u.ref) === String(editing.id) && u.role === "parent"); const oldStudent = { ...editing }; const patch = { name: form.name.trim(), sid: form.sid.trim(), cls: form.cls, sec: form.sec, parentname: form.parentName.trim(), parentphone: parentPhone, enroll: form.enroll, status: form.status };
          try { await updR("students", editing.id, patch); const sa = await provision("student", patch.sid, patch.name, String(editing.id), "update", su?.auth_id ? String(su.auth_id) : null, form.password || undefined); const pa = await provision("parent", parentPhone, patch.parentname, String(editing.id), "update", pu?.auth_id ? String(pu.auth_id) : null); if (sa.authId) { const p = { name: patch.name, phone: patch.sid, email: sa.email, status: form.status, auth_id: sa.authId }; if (su) await updR("users", su.id, p); else await addR("users", { id: `u-${sa.authId}`, ...p, role: "student", ref: editing.id }); } if (pa.authId) { const p = { name: patch.parentname, phone: parentPhone, email: pa.email, status: form.status, auth_id: pa.authId }; if (pu) await updR("users", pu.id, p); else await addR("users", { id: `u-${pa.authId}`, ...p, role: "parent", ref: editing.id }); await updR("students", editing.id, { parent: pa.authId }); } } catch (e) { try { await updR("students", editing.id, { name: oldStudent.name, sid: oldStudent.sid, cls: oldStudent.cls, sec: oldStudent.sec, parentname: oldStudent.parentname, parentphone: oldStudent.parentphone, enroll: oldStudent.enroll, status: oldStudent.status, parent: oldStudent.parent }); } catch (rollbackError) { console.error("Student record rollback failed:", rollbackError); } throw e; }
          setSuccess("Student and linked accounts updated successfully.");
        } else {
          const phone = normalizePhone(form.phone ?? ""); if (!phone || !teacherSubjects.length || !teacherClasses.length) throw new Error("Fill Name, Phone, select at least one Subject, and assign at least one Class."); if (form.password) validatePassword(form.password, "Teacher password");
          const users = (await gdb("users")) as Row[]; const u = users.find((x) => String(x.ref) === String(editing.id) && x.role === "teacher"); const oldTeacher = { ...editing }; const patch = { name: form.name.trim(), tid: form.tid.trim(), subject: teacherSubjects.join(", "), phone, status: form.status, classes: teacherClasses };
          try { await updR("teachers", editing.id, patch); const a = await provision("teacher", phone, patch.name, String(editing.id), "update", u?.auth_id ? String(u.auth_id) : null, form.password || undefined); if (a.authId) { const p = { name: patch.name, phone, email: a.email, status: form.status, auth_id: a.authId }; if (u) await updR("users", u.id, p); else await addR("users", { id: `u-${a.authId}`, ...p, role: "teacher", ref: editing.id }); } } catch (e) { try { await updR("teachers", editing.id, { name: oldTeacher.name, tid: oldTeacher.tid, subject: oldTeacher.subject, phone: oldTeacher.phone, status: oldTeacher.status, classes: oldTeacher.classes }); } catch (rollbackError) { console.error("Teacher record rollback failed:", rollbackError); } throw e; }
          setSuccess("Teacher and linked account updated successfully.");
        }
      } else if (isStudent) {
        const parentPhone = normalizePhone(form.parentPhone ?? ""); if (!form.cls || !form.sec || !form.parentName?.trim() || !parentPhone) throw new Error("Fill all required student and parent fields."); const studentPassword = form.password || DEFAULT_STUDENT_PASSWORD; validatePassword(studentPassword, "Student password"); const sid = form.sid.trim(); const student = await addR("students", { id: `s-${Date.now()}`, name: form.name.trim(), sid, cls: form.cls, sec: form.sec, parentname: form.parentName.trim(), parentphone: parentPhone, enroll: form.enroll, status: form.status || "active" }); const created: Array<{ role: ProvisionRole; authId?: string; login: string; name: string; ref: string }> = [];
        try { const sa = await provision("student", sid, form.name.trim(), String(student.id), "create", null, studentPassword); created.push({ role: "student", authId: sa.authId, login: sid, name: form.name.trim(), ref: String(student.id) }); const pa = await provision("parent", parentPhone, form.parentName.trim(), String(student.id), "create", null, DEFAULT_PARENT_PASSWORD); created.push({ role: "parent", authId: pa.authId, login: parentPhone, name: form.parentName.trim(), ref: String(student.id) }); if (!sa.authId || !pa.authId) throw new Error("Authentication accounts were not returned by the provisioning service."); await updR("students", student.id, { parent: pa.authId }); await addR("users", { id: `u-${sa.authId}`, name: form.name.trim(), phone: sid, email: sa.email, role: "student", ref: student.id, status: form.status || "active", auth_id: sa.authId }); await addR("users", { id: `u-${pa.authId}`, name: form.parentName.trim(), phone: parentPhone, email: pa.email, role: "parent", ref: student.id, status: form.status || "active", auth_id: pa.authId }); setCredentials({ role: "Student", login: sid, password: studentPassword }); setSuccess(`Student ${form.name.trim()} created with SID ${sid}.`); } catch (e) { await rollbackAuth(created); try { await delR("students", student.id); } catch (rollbackError) { console.error("Student record rollback failed:", rollbackError); } throw new Error(`Account creation failed. Nothing was left behind: ${e instanceof Error ? e.message : "Unknown error"}`); }
      } else {
        const phone = normalizePhone(form.phone ?? ""); if (!phone || !teacherSubjects.length || !teacherClasses.length) throw new Error("Fill Name, Phone, select at least one Subject, and assign at least one Class."); const password = form.password || DEFAULT_TEACHER_PASSWORD; validatePassword(password, "Teacher password"); const teacher = await addR("teachers", { id: `t-${Date.now()}`, name: form.name.trim(), tid: form.tid, subject: teacherSubjects.join(", "), phone, classes: teacherClasses, status: "active" }); const created: Array<{ role: ProvisionRole; authId?: string; login: string; name: string; ref: string }> = [];
        try { const a = await provision("teacher", phone, form.name.trim(), String(teacher.id), "create", null, password); created.push({ role: "teacher", authId: a.authId, login: phone, name: form.name.trim(), ref: String(teacher.id) }); if (!a.authId) throw new Error("Authentication account was not returned by the provisioning service."); await addR("users", { id: `u-${a.authId}`, name: form.name.trim(), phone, email: a.email, role: "teacher", ref: teacher.id, status: "active", auth_id: a.authId }); setCredentials({ role: "Teacher", login: phone, password }); setSuccess(`Teacher ${form.name.trim()} created successfully.`); } catch (e) { await rollbackAuth(created); try { await delR("teachers", teacher.id); } catch (rollbackError) { console.error("Teacher record rollback failed:", rollbackError); } throw new Error(`Account creation failed. Nothing was left behind: ${e instanceof Error ? e.message : "Unknown error"}`); }
      }
      closeForm(); await load();
    } catch (e) { setError(e instanceof Error ? e.message : "Unable to save record."); } finally { setSaving(false); }
  };
  const remove = async (row: Row) => { if (!window.confirm(isStudent ? "Delete this student and both linked accounts permanently?" : "Delete this teacher and their login account permanently?")) return; setSaving(true); setError(""); setSuccess(""); setCredentials(null); try { const users = (await gdb("users")) as Row[]; const roles = isStudent ? ["student", "parent"] : ["teacher"]; const linked = users.filter((u) => String(u.ref) === String(row.id) && roles.includes(String(u.role))); for (const u of linked) { if (!u.auth_id) continue; await provision(String(u.role) as ProvisionRole, String(u.phone ?? (isStudent ? row.sid : row.tid)), String(u.name ?? row.name ?? "User"), String(row.id), "delete", String(u.auth_id)); } for (const u of linked) await delR("users", u.id); await delR(kind, row.id); setRows((r) => r.filter((x) => x.id !== row.id)); setSuccess(isStudent ? "Student and linked accounts deleted permanently." : "Teacher and linked account deleted permanently."); } catch (e) { setError(e instanceof Error ? e.message : "Delete failed. No database record was removed unless the operation completed successfully."); } finally { setSaving(false); } };
  const hasForm = Object.keys(form).length > 0;
  return <div style={{ padding: 28, background: "#F7F9FF", minHeight: "100%" }}>{success && <div style={{ ...card, padding: 13, marginBottom: 16, background: "#F0FDF4", color: "#16A34A", fontWeight: 700 }}>{success}</div>}{credentials && <div style={{ ...card, padding: 16, marginBottom: 16, background: "#EFF6FF", color: "#1E40AF" }}><div style={{ fontWeight: 800, marginBottom: 6 }}>🔐 One-time login details</div><div><strong>{credentials.role} Login ID:</strong> {credentials.login}</div><div><strong>Temporary password:</strong> {credentials.password}</div><div style={{ fontSize: 11, marginTop: 7 }}>This password is not stored in the database. Copy it now and give it securely to the user.</div></div>}{error && <div style={{ ...card, padding: 13, marginBottom: 16, background: "#FFF7F7", color: C.red, fontWeight: 650 }}>{error}</div>}<div style={{ display: "flex", gap: 12, justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", marginBottom: 20 }}><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="🔍 Search by name or ID…" style={{ flex: "1 1 280px", padding: "11px 16px", borderRadius: 12, border: `1.5px solid ${C.border}`, background: "#fff" }} /><button type="button" disabled={saving} onClick={() => void openNew()} style={{ border: 0, borderRadius: 12, padding: "11px 16px", background: C.accent, color: "#fff", fontWeight: 800 }}>{isStudent ? "🎓 + Add Student" : "👨‍🏫 + Add Teacher"}</button></div><div style={{ ...card, overflow: "hidden" }}>{loading ? <div style={{ padding: 30, color: C.sub }}>Loading records…</div> : !filtered.length ? <div style={{ padding: 45, textAlign: "center", color: C.sub }}>No records found.</div> : <div style={{ overflowX: "auto" }}><table style={{ width: "100%", borderCollapse: "collapse", minWidth: isStudent ? 850 : 780 }}><thead><tr>{(isStudent ? ["SID", "Student Name", "Class", "Parent", "Parent Phone", "Status", "Actions"] : ["Teacher ID", "Name", "Subject", "Phone", "Classes", "Status", "Actions"]).map((h) => <th key={h} style={{ padding: "12px 14px", background: "#F8FAFF", textAlign: "left", fontSize: 12, color: C.sub }}>{h}</th>)}</tr></thead><tbody>{filtered.map((r) => <tr key={String(r.id)} style={{ borderTop: `1px solid ${C.border}` }}>{isStudent ? <><td style={{ padding: 12 }}>{String(r.sid)}</td><td style={{ padding: 12, fontWeight: 700 }}>{String(r.name)}</td><td style={{ padding: 12 }}>{String(r.cls)}-{String(r.sec)}</td><td style={{ padding: 12 }}>{String(r.parentname ?? "—")}</td><td style={{ padding: 12 }}>{String(r.parentphone ?? "—")}</td><td style={{ padding: 12 }}>{String(r.status ?? "active")}</td></> : <><td style={{ padding: 12 }}>{String(r.tid)}</td><td style={{ padding: 12, fontWeight: 700 }}>{String(r.name)}</td><td style={{ padding: 12 }}>{String(r.subject ?? "—")}</td><td style={{ padding: 12 }}>{String(r.phone ?? "—")}</td><td style={{ padding: 12 }}>{Array.isArray(r.classes) ? r.classes.join(", ") : "—"}</td><td style={{ padding: 12 }}>{String(r.status ?? "active")}</td></>}<td style={{ padding: 12, whiteSpace: "nowrap" }}><button type="button" disabled={saving} onClick={() => openEdit(r)} style={{ border: `1px solid ${C.accent}`, background: "transparent", color: C.accent, borderRadius: 8, padding: "7px 10px", marginRight: 6 }}>Edit</button><button type="button" disabled={saving} onClick={() => void remove(r)} style={{ border: 0, background: "#FFF1F2", color: C.red, borderRadius: 8, padding: "7px 10px" }}>Delete</button></td></tr>)}</tbody></table></div>}</div>{hasForm && <div onClick={closeForm} style={{ position: "fixed", inset: 0, background: "rgba(15,27,61,.6)", zIndex: 100, display: "grid", placeItems: "center", padding: 16 }}><div onClick={(e) => e.stopPropagation()} style={{ background: "#fff", borderRadius: 22, width: "min(900px,100%)", maxHeight: "92vh", overflowY: "auto", padding: 26 }}><div style={{ display: "flex", justifyContent: "space-between", marginBottom: 20 }}><h2 style={{ margin: 0 }}>{editing ? `Edit ${isStudent ? "Student" : "Teacher"}` : `Add New ${isStudent ? "Student" : "Teacher"}`}</h2><button type="button" onClick={closeForm}>✕</button></div>{isStudent ? <><div style={{ background: "#F0F9FF", borderRadius: 14, padding: 13, marginBottom: 18, color: "#0284C7", fontSize: 13 }}>🔐 Passwords are managed by Supabase Auth and are never stored in the database. {editing ? "Leave Student Password blank to keep the current password." : `Default student password: ${DEFAULT_STUDENT_PASSWORD}. Parent password: ${DEFAULT_PARENT_PASSWORD}.`}</div><div style={{ display: "grid", gridTemplateColumns: "repeat(2,minmax(0,1fr))", gap: 12 }}><Input label="Full Name" value={form.name ?? ""} onChange={(v) => set("name", v)} required /><Input label="SID / Login ID" value={form.sid ?? ""} onChange={(v) => set("sid", v)} disabled required note="Format: LG-001" /><Select label="Class" value={form.cls ?? ""} onChange={(v) => set("cls", v)} options={CLASSES} required /><Select label="Section" value={form.sec ?? ""} onChange={(v) => set("sec", v)} options={SECTIONS} required /><Input label="Enrollment Date" type="date" value={form.enroll ?? ""} onChange={(v) => set("enroll", v)} /><Select label="Status" value={form.status ?? "active"} onChange={(v) => set("status", v)} options={["active", "inactive"]} /><Input label={editing ? "New Student Password (optional)" : "Student Password"} value={form.password ?? ""} onChange={(v) => set("password", v)} placeholder={editing ? "Leave blank to keep current" : DEFAULT_STUDENT_PASSWORD} type="password" required={!editing} /></div><div style={{ marginTop: 18, fontWeight: 800 }}>👨‍👩‍👧 Parent Information</div><div style={{ display: "grid", gridTemplateColumns: "repeat(2,minmax(0,1fr))", gap: 12, marginTop: 12 }}><Input label="Parent / Guardian Name" value={form.parentName ?? ""} onChange={(v) => set("parentName", v)} required /><Input label="Parent Phone (Login)" value={form.parentPhone ?? ""} onChange={(v) => set("parentPhone", normalizePhone(v))} required note="Use the same phone as the parent's login ID." /></div></> : <><div style={{ background: "#F0F9FF", borderRadius: 14, padding: 13, marginBottom: 18, color: "#0284C7", fontSize: 13 }}>🔐 Passwords are managed by Supabase Auth and are never stored in the database. {editing ? "Leave the password blank to keep the current password." : `Default teacher password: ${DEFAULT_TEACHER_PASSWORD}.`} Teachers must have at least one subject and one assigned class.</div><div style={{ display: "grid", gridTemplateColumns: "repeat(2,minmax(0,1fr))", gap: 12 }}><Input label="Full Name" value={form.name ?? ""} onChange={(v) => set("name", v)} required /><Input label="Teacher ID" value={form.tid ?? ""} onChange={(v) => set("tid", v)} disabled required /><Input label="Phone (Login ID)" value={form.phone ?? ""} onChange={(v) => set("phone", normalizePhone(v))} required /><Input label={editing ? "New Password (optional)" : "Password"} value={form.password ?? ""} onChange={(v) => set("password", v)} type="password" required={!editing} placeholder={editing ? "Leave blank to keep current" : DEFAULT_TEACHER_PASSWORD} /><Select label="Status" value={form.status ?? "active"} onChange={(v) => set("status", v)} options={["active", "inactive"]} /></div><div style={{ marginTop: 14 }}><MultiSelect label="Subjects (select one or more)" value={teacherSubjects} onChange={setTeacherSubjects} options={TEACHER_SUBJECTS} /></div><div style={{ marginTop: 8, fontSize: 11, color: C.sub }}>Includes Classes 9-10 core subjects and common Classes 11-12 Commerce subjects.</div><div style={{ marginTop: 14 }}><MultiSelect label="Assigned Classes (select one or more)" value={teacherClasses} onChange={setTeacherClasses} options={CLASSES.map((c) => `Class ${c}`)} /></div></>}
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 10, marginTop: 22 }}><button type="button" onClick={closeForm} style={{ border: `1px solid ${C.sub}`, background: "transparent", borderRadius: 10, padding: "10px 16px" }}>Cancel</button><button type="button" disabled={saving} onClick={() => void save()} style={{ border: 0, background: C.accent, color: "#fff", borderRadius: 10, padding: "10px 16px", fontWeight: 800 }}>{saving ? "Saving…" : editing ? "Save Changes" : isStudent ? "Add Student + Create Accounts" : "Add Teacher + Create Account"}</button></div>
      </div></div>}
    </div>
  );
}